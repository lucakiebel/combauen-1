import greenfoot.*;
import java.awt.Color;
/**
 * Write a description of class Anz here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Anz extends Actor 
{
    /*
    public Anz(String text) {
        
        GreenfootImage image = new GreenfootImage(text.length()*20, 20);
        image.drawString(text,255,12);
        
        setImage(image);
    }
    
    public void update(String text) {
    
        GreenfootImage image = getImage();
        image.clear();
        image.drawString(text,255, 12);
    }
    */
}
